import { LineBreakPipePipe } from './line-break-pipe.pipe';

describe('LineBreakPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LineBreakPipePipe();
    expect(pipe).toBeTruthy();
  });
});
